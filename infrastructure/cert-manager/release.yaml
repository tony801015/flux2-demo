apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cert
spec:
  releaseName: cert-manager
  chart:
    spec:
      chart: jetstack
      sourceRef:
        kind: HelmRepository
        name: jetstack
        namespace: flux-system
      version: "v0.15.0"
  interval: 1m
  install:
    remediation:
      retries: 3
